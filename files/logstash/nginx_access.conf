input {
  file {
    path => [ "/var/log/nginx/access.log" ]
  }
}

output {
  stdout {  }
  elasticsearch {
    protocol => "http"
    bind_host => "104.130.154.202"
    index => "logstash-gpuz-%{+YYYY.MM.dd}"
  }
}

